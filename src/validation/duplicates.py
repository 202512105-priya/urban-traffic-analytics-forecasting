import pandas as pd

def count_duplicates(df: pd.DataFrame) -> int:
    """
    TODO: Count exact duplicate rows.
    
    Args:
        df (pd.DataFrame): Dataset.
    Returns:
        int: Number of duplicate rows.
    """
    raise NotImplementedError("count_duplicates() is not implemented yet.")

def duplicate_rows(df: pd.DataFrame) -> pd.DataFrame:
    """
    TODO: Filter and return only the duplicate rows.
    
    Args:
        df (pd.DataFrame): Dataset.
    Returns:
        pd.DataFrame: Duplicate records.
    """
    raise NotImplementedError("duplicate_rows() is not implemented yet.")

def duplicate_percentage(df: pd.DataFrame) -> float:
    """
    TODO: Calculate percentage of rows that are duplicates.
    
    Args:
        df (pd.DataFrame): Dataset.
    Returns:
        float: Duplicate percentage.
    """
    raise NotImplementedError("duplicate_percentage() is not implemented yet.")

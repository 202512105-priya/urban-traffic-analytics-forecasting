import pandas as pd

def train_model(X: pd.DataFrame, y: pd.Series):
    """
    TODO: Train general model (e.g. Random Forest, XGBoost) and return it.
    """
    raise NotImplementedError("train_model() is not implemented yet.")

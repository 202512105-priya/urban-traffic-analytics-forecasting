import pandas as pd
from typing import Dict

def evaluate_forecast(y_true: pd.Series, y_pred: pd.Series) -> Dict[str, float]:
    """
    TODO: Compute forecasting error matrices (MAPE, etc.).
    """
    raise NotImplementedError("evaluate_forecast() is not implemented yet.")

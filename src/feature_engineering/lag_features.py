import pandas as pd

def create_lags(df: pd.DataFrame, group_col: str, target_col: str, lags: list) -> pd.DataFrame:
    """
    TODO: Shift target values per junction.
    """
    raise NotImplementedError("create_lags() is not implemented yet.")

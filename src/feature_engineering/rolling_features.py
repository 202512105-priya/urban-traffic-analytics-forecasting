import pandas as pd

def create_rolling_means(df: pd.DataFrame, group_col: str, target_col: str, windows: list) -> pd.DataFrame:
    """
    TODO: Compute rolling averages.
    """
    raise NotImplementedError("create_rolling_means() is not implemented yet.")
